Not the lab's logo.

`aura-logo.svg` and `aura-glyph.svg` — four gradient arcs with a serif A — were
found in the old site repo and used in an early pass of the rebuild by mistake.
The real mark is the circuit-tree glyph in the parent directory. Kept only so the
old GitHub Pages site keeps rendering until the redirects land.
