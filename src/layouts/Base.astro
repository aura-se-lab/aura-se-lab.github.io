---
import "@/styles/global.css";
import Seo from "@/components/Seo.astro";
import Nav from "@/components/Nav.astro";
import Footer from "@/components/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: "website" | "article" | "profile";
  meta?: { name: string; content: string }[];
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  noindex?: boolean;
  publishedTime?: string;
  class?: string;
  /** Set false on the single-screen landing page, which carries its own rail. */
  footer?: boolean;
  /** Set false for pages rendered inside the reader overlay. */
  nav?: boolean;
}
const { class: cls, footer = true, nav = true, ...seo } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <Seo {...seo} />
  </head>
  <body class={cls}>
    {nav && <a class="skip-link" href="#main">Skip to content</a>}
    {nav && <Nav />}
    <main id="main">
      <slot />
    </main>
    {footer && <Footer />}
  </body>
</html>
